import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "GrowEasy CSV Importer — AI-Powered CRM Lead Extraction",
  description:
    "Upload any CSV format and let AI intelligently extract and map your data into GrowEasy CRM-ready records. Supports Facebook Leads, Google Ads exports, Excel sheets, and more.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
